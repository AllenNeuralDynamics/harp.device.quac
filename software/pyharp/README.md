# Python Example Suite

## Pre-requisites
You must have [uv](https://docs.astral.sh/uv/) installed.

## Running Examples
These examples are self-contained scripts.
To run them simply invoke them with uv like so:

```bash
uv run wait_for_events.py
```

